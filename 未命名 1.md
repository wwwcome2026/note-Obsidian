 1. # 是占位符，$是赋值符，#能有效放在sql注入
 2. 实体类使用@TableField注解声明数据库字段名 在数据库中为字段起别名as
 3. select * from 表名 like 
 4. 使用LambdaUpdataWrapper,.set()
 5. select id from 表名 where 字段
 6. @Param 
 7. 查询组合条件时，根据传入的条件是否为空，决定是否添加
 8. 不同可以，id是用来映射方法的。决定类与xml映射关系的是namespace
 9. 加载配置文件，创建工厂，打开通道。
 10. 原生limit：limit pageSize（pageNum-1）* page size
 11. 