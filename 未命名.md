pageNum
pageSize

list
total
pages

select count( * ) from user where status =1;
select * from user wher status =1 order by id limit 10 offset 20;

# Mapper XML
```java
<select id="selectPageList" resultType= "UserVo">
	select count( * ) from user
	<where>
		<if test="quer.username !=null and query.username!= "">
		 and username like concat('%',# {query.username},'%')
		</if>
		<if test="query.status != null">
            AND status = #{query.status}
        </if>
    </where>
    order by creat_time desc
    limit #{pageSize} offset #{pageNum}
		
```
# Service 
```java
	@Autowired
	privat UserMapper usermapper
	@Override
	public PageList<UserVo> listByPage(Integer pageNum,Integer pageSize,UserDto userDto){
		// 查询总数
		int total=userMapper.selectCount(userDto);
		// 偏移量
		int offset= （pageNum-1) * pageSize;
		// 查询当前页数据
		List<UserVo> list=userMapper.selectPageList(offset,userDto.pageSize);
		// 封装数据返回
		return PageResult.of(total,pageNum,pageSize,list);
		
	}
```
# PageResult封装
```java
@Data
public class PageResult<T>{
	private Long total;总条数
	pages 总页数
	pageSize 每页条数
	pageNum 当前页
	list 数据列表
	private List<T> list
	\
	public static <T> PageResult<T> of(total,pageNum,pageSize,List<T> list){
		PageResult<T> result =
	}
	
}
```